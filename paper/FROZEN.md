# FROZEN (2026-06-24)

This conference cut is **no longer maintained**. The single maintained
paper is **`report/main.tex`** (the T-RO journal version), which is a
content superset of this directory.

- Do **not** edit files here.
- All fixes and content changes go into `report/` only.
- This directory is kept in git history as a record / starting point if a
  conference (ICRA/IROS-style) version is ever needed; regenerate it from
  `report/` at that time rather than reviving this copy.
