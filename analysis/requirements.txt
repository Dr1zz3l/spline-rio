# Python version requirement for uv
# requires-python==3.12

# Core scientific Python stack
numpy>=1.24.0
pandas>=2.0.0
matplotlib>=3.7.0
seaborn>=0.12.0

# ROS bag processing
# Note: rosbag, rospkg, rospy are provided by ROS installation
# They should be accessed by sourcing ROS: source /opt/ros/noetic/setup.bash
# The following are included but may need ROS environment setup:
rospkg
bagpy
rosbags>=0.9.0

# Jupyter
jupyter>=1.0.0
ipython>=8.0.0
ipywidgets>=8.0.0

# Additional utilities
scipy>=1.10.0
scikit-learn>=1.3.0
plotly>=5.14.0
open3d>=0.18.0

ipykernel
pycryptodomex
python-gnupg

# symforce